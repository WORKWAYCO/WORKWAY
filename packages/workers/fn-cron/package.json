{
  "name": "fn-cron",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker cron trigger for fn.workway.co auto-sync",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.114.0"
  }
}
