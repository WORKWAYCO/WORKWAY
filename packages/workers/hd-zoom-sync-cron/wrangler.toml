name = "hd-zoom-sync-cron"
main = "src/index.ts"
compatibility_date = "2025-11-09"
compatibility_flags = ["nodejs_compat"]
account_id = "5c3e9cf4d55ce171b844fad0931607f9"

# Runs at 13:00 and 14:00 UTC daily. Worker checks America/Chicago time and only
# executes when local time is exactly 08:00 (DST-safe).
[triggers]
crons = ["0 13 * * *", "0 14 * * *"]

[vars]
MEETINGS_BASE_URL = "https://meetings.workway.co"
USER_ID = "dm-halfdozen-co"
RESEND_TO = "micah@createsomething.io"
RESEND_FROM = "WORKWAY <newsletter@workway.co>"

