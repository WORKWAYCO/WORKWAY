{
  "name": "@workway/zoom-cookie-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Per-user Zoom cookie storage for Private Workflow (bookmarklet auth)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.15"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.99.0"
  }
}
